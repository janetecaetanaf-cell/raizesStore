[phases.setup]
nixPkgs = ["dotnet-sdk_8", "dotnet-runtime_8"]

[phases.build]
cmds = ["dotnet publish RaizesStore.Api/RaizesStore.Api.csproj -c Release -o /app/publish"]

[start]
cmd = "dotnet /app/publish/RaizesStore.Api.dll"
